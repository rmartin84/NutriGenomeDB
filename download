require 'csv'
  @target="GSE18589 MYOTUBES EICOSAPENTAENOIC ACID 24H.xls"
  @target1= File.read(@target)

  archivo=CSV.parse(@target1, :headers => true, :col_sep => "\t")
  archivo1=archivo["PROBE"]

  	File.open("prueba.xls", "w") do |file1|
	archivo1.each do |vt|	
	file1.puts (vt)
end
end



